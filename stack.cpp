/*#include <iostream>
#include <stack>

using namespace std;

void stackF() {
	stack<int> myStack;
	
	myStack.push(5);
	myStack.push(3);
	myStack.push(2);
	
	cout << "Number of ints on the stack " << myStack.size() << endl;
	
	while(!myStack.empty()) {
		cout << "popping " << myStack.top() << endl;
		//cout << "popping " << myStack.top() << endl;
		myStack.pop();
	}
	cout << "Number of ints on the stack " << myStack.size() << endl;
}*/
